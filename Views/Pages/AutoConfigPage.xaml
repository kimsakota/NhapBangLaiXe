<Page x:Class="ToolVip.Views.Pages.AutoConfigPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:ToolVip.Views.Pages"
      xmlns:viewmodels="clr-namespace:ToolVip.ViewModels.Pages"
      
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AutoConfigPage">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TextBlock Text="Cấu hình Tự động (OCR)" 
                      FontTypography="TitleLarge" 
                      Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" >
            <!-- Cài đặt Bật/Tắt OCR -->
            <ui:CardControl Icon="{ui:SymbolIcon ScanText24}">
                <ui:CardControl.Header>
                    <StackPanel>
                        <TextBlock Text="Kích hoạt giám sát OCR" FontWeight="Medium"/>
                        <TextBlock Text="Tự động đọc màn hình để phát hiện lỗi/captcha" 
                                   Foreground="Gray" FontSize="12"/>
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:ToggleSwitch IsChecked="{Binding IsOcrEnabled, Mode=TwoWay}"/>
            </ui:CardControl>

            <!-- Cài đặt Từ khóa -->
            <ui:CardControl Icon="{ui:SymbolIcon Key24}">
                <ui:CardControl.Header>
                    <StackPanel>
                        <TextBlock Text="Từ khóa dừng chạy" FontWeight="Medium"/>
                        <TextBlock Text="Auto sẽ dừng ngay lập tức nếu thấy từ này (ví dụ: Stop, Lỗi...)" 
                                   Foreground="Gray" FontSize="12"/>
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:TextBox Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                            PlaceholderText="Nhập từ khóa..." 
                            Width="250"/>
            </ui:CardControl>
        </StackPanel>
    </Grid>
</Page>