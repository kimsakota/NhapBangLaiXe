<Page
    x:Class="ToolVip.Views.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ToolVip.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:models="clr-namespace:ToolVip.Models"
    Title="DashboardPage"
    d:DataContext="{d:DesignInstance local:DashboardPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="650"
    d:DesignWidth="500"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">

    <Grid VerticalAlignment="Top">
        <ui:ListView Grid.Row="1" 
                     d:ItemsSource="{d:SampleData ItemCount=5}"
                     ItemsSource="{Binding ViewModel.Profiles}" 
                     SelectionMode="Single"
                     x:Name="DriverListView"
                     >
            <ui:ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type models:DriverProfile}">
                    
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <ui:SymbolIcon Symbol="Person24" FontSize="24" Margin="0,0,15,0"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding LicensePlate}" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="{Binding FullName}" Foreground="Gray"/>
                            </StackPanel>
                        <ui:Button Grid.Column="2" 
                                   Icon="{ui:SymbolIcon Symbol=Eye24}"
                                   MouseOverBackground="LightBlue"
                                   Command="{Binding OnOpenDetailCommand}"/>

                    </Grid>
                    
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>


    </Grid>
</Page>
